# Cognitive Graph Agent — .env
# Copy file này thành .env và điền các giá trị thực

# ── LLM ────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
# MODEL=gpt-4o-mini

# ── HTTP Basic Auth ────────────────────────────────────────────────
# Bỏ trống = không cần đăng nhập (chạy local)
# Điền password = bật auth (deploy lên server)
APP_USERNAME=admin
APP_PASSWORD=
